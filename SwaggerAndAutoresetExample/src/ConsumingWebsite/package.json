{
  "name": "website",
  "description": "Used for generation of AutoRest client only",
  "scripts": {
    "generate": "autorest -n ConsumingWebsite.Clients.DataService -i http://localhost:5200/swagger/doc/swagger.json -o Clients.DataService"
  },
  "devDependencies": {
    "autorest": "^0.13.2"
  }
}
